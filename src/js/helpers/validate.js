/**
 * Validate function. Check Input on RegExp provided in regExpDic by input data-required type.
 * @param {HTMLInputElement} el
 * @returns {Boolean} - Return true if input is valid or doesn't have data-required attr.
 */

export function validate(el) {}
